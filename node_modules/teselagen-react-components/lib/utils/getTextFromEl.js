"use strict";

exports.__esModule = true;
exports.default = getTextFromEl;
function getTextFromEl(el) {
  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var lowerCase = options.lowerCase;


  return el.props && el.props.children ? (el.props.children.reduce ? el.props.children : [el.props.children]).reduce(function (acc, child) {
    if (child && child.props && child.props.children) {
      acc += getTextFromEl(child);
    } else if (typeof child === "string") {
      if (lowerCase) {
        acc += child.toLowerCase();
      } else {
        acc += child;
      }
    } else if (typeof child === "number") {
      acc += child + "";
    }
    return acc;
  }, "") : "";
}
module.exports = exports["default"];