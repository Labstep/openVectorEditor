import React from "react";

import { Spinner } from "@blueprintjs/core";

import "./loader.css";

var Loader = function Loader() {
  return React.createElement(
    "div",
    { className: "mss-loader" },
    React.createElement(Spinner, { style: { width: 100 } })
  );
};

export default Loader;