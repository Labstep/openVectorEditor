import React from "react";
import PropTypes from "prop-types";

import { Button } from "@blueprintjs/core";
import ItemLabel from "./item_label";

import "./selected_item.css";

var SelectedItem = function SelectedItem(_ref) {
  var item = _ref.item,
      height = _ref.height;
  return React.createElement(
    "div",
    { className: "mss-selected_item", style: { height: height } },
    React.createElement(ItemLabel, { label: item.label }),
    React.createElement(Button, { style: { marginRight: 10 }, minimal: true, icon: "cross" })
  );
};

SelectedItem.propTypes = process.env.NODE_ENV !== "production" ? {
  item: PropTypes.object,
  height: PropTypes.number
} : {};

SelectedItem.defaultProps = {
  item: {},
  height: 40
};

export default SelectedItem;