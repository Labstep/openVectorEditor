import React from "react";
import PropTypes from "prop-types";

import "./no_items.css";

var NoItems = function NoItems(_ref) {
  var noItemsMessage = _ref.noItemsMessage;
  return React.createElement(
    "div",
    { className: "mss-no_items" },
    noItemsMessage
  );
};

NoItems.propTypes = process.env.NODE_ENV !== "production" ? {
  noItemsMessage: PropTypes.string
} : {};

NoItems.defaultProps = {
  noItemsMessage: "No Items..."
};

export default NoItems;